name: Keep HF Space Awake

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Acronous AI
        run: curl -s -o /dev/null -w "%{http_code}" https://hritesh0214-acronous-ai.hf.space/v1/health
