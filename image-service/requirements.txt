fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
Pillow>=10.1.0
numpy>=1.24.0
rembg[cpu]>=0.1.0
opencv-python-headless>=4.8.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
loguru>=0.7.2
pydantic>=2.5.0
# Voice TTS (Microsoft Neural TTS — 300+ voices, no API key needed)
edge-tts>=6.1.0
# Video generation: proper cinematic transitions, camera movements, 3D effects
moviepy>=1.0.3
# Voice/audio editing
pydub>=0.25.1
soundfile>=0.12.0
# Async HTTP for Ollama vision integration
aiohttp>=3.9.0

# Local diffusion (free, unlimited, self-hosted photorealistic generation on CPU)
# NOTE: torch is installed separately in the Dockerfile from the CPU-only index
# to avoid pulling the multi-GB CUDA build.
diffusers==0.30.1
transformers==4.45.2
accelerate==1.0.1
safetensors==0.4.5
huggingface_hub==0.25.2
